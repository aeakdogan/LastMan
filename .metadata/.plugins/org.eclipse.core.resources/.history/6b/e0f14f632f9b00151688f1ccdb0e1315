import java.util.ArrayList;

import javax.swing.JFrame;

public class Framework {
	private static JFrame frame;
	
	static int MAIN_MENU_ID = 0;
	static int SETTINGS_MENU_ID = 1;
	
	private static ArrayList<JPanel> menu;
	
	public Framework(JFrame frame){
		Framework.frame = frame;
		frame.setTitle("Last Man");
		frame.setSize(1100,600);
		frame.setLocationRelativeTo(null);
		frame.setResizable(false);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		
		
		
		
		menu = new ArrayList<Menu>();
		menu.add(new MainMenu());
		menu.add(new SettingsView(new Settings()));
		
		frame.setContentPane(menu.get(MAIN_MENU_ID));
		frame.setVisible(true);
	}
	
	public static void switchMenu(int menuID){
		frame.setContentPane(menu.get(menuID));
		frame.invalidate();
		frame.validate();
	}
}
